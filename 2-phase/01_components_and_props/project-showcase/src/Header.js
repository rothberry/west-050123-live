// // import projects from "./projects.js"

const Header = (props) => {
	// console.log(props)
	return (
		<header>
			We got {props.amount} Projects
			{/* {2 + 2}
			{projects[0].name}
			{true} */}
		</header>
	)
}

export default Header
